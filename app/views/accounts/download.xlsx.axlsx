wb = xlsx_package.workbook
wb.add_worksheet(name: "帐号") do |sheet|
  sheet.add_row ["手机帐号", "密码","数据", "时间", "短信api"]
  @accounts.each do |account|
    sheet.add_row [account.phone, account.password,account.token, account.time, account.link]
  end
end